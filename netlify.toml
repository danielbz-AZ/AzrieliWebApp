[build]
  command = "npm run build"
  publish = "public"  # Folder to publish (change if your HTML is in a different folder)
  functions = "functions"  # Folder containing serverless functions

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404