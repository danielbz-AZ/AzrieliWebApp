[build]
  command = "npm run build"
  publish = "dist"  # Folder to publish (change if your HTML is in a different folder)
  functions = "/netlify/functions"  # Folder containing serverless functions

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404